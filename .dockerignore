# 排除两个容器都不需要的文件（web 和 api 共用同一个 build context）
# 注意：requirements.txt / mkdocs.yml / nginx.conf 由 web(Dockerfile) 使用，不能排除
# 注意：docs/ 由 api(Dockerfile) 使用（COPY docs/ ./docs_baked/），不能排除
site/
.git/
.venv/
venv/
__pycache__/
*.py[cod]
*.log
.local_exam_data/
.env
.env.*
*.db
*.sqlite
*.sqlite3
deploy_local_or_coolify.py
